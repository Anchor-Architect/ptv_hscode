# 이 파일을 복사해서 `.env` 로 저장한 뒤, 아래 값을 실제 키로 바꾸세요.
#   cp .env.example .env
# .env 는 절대 git 에 커밋하지 마세요 (.gitignore 에 이미 제외되어 있습니다).

# Anthropic API 키 (https://console.anthropic.com 에서 발급)
ANTHROPIC_API_KEY=sk-ant-여기에-실제-키

# (선택) 서버 포트. 기본 8000
# PORT=8000
